<?xml version="1.0" encoding="UTF-8" ?>

<extension id="db_charsetter" xmlns="http://symphony-cms.com/extensions/1.0">

	<!-- string, required -->
	<name>Database Character Setter</name>

	<!-- string, required (@lang optional) -->
	<description lang="en">Migrate any non-utf8 database content to utf8.</description>

	<!-- string, required (@type=github|codebase|other, for future repo posibilities?) (must be Github for clever API things to happen) -->
	<repo type="github">https://github.com/symphonists/db_charsetter</repo>

	<!-- string, optional (@type=homepage|wiki|other?) -->
	<url type="homepage">http://symphony-cms.com/downloads/extensions/db_charsetter</url>

	<!-- list, optional (Symphony to define suggested vocabulary, suggest leaving this out initially?) -->
	<types>
		<type>Workflow</type>
		<type>Other</type>
	</types>

	<!-- list, required -->
	<authors>
		<author>
			<!-- string, required (@github, @symphony, @twitter optional) -->
			<name github="symphonists" symphony="symphonists">Symphonists</name>
		</author>
		<author>
			<!-- string, required (@github, @symphony, @twitter optional) -->
			<name github="designermonkey" symphony="designermonkey">John Porter</name>
		</author>
	</authors>

	<!-- list, required at least one. Largest version number is assumed latest -->
	<releases>
		<release version="0.1" date="2012-03-010" min="2.3" />
	</releases>

</extension>